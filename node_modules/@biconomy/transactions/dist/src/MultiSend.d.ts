import { Contract } from 'ethers';
import { IMetaTransaction, IWalletTransaction } from '@biconomy/core-types';
export declare const encodeMultiSend: (txs: IMetaTransaction[]) => string;
export declare const buildMultiSendSmartAccountTx: (multiSend: Contract, txs: IMetaTransaction[], nonce: number, overrides?: Partial<IWalletTransaction>) => IWalletTransaction;
