import { IFallbackAPI, FallbackUserOperation, FallbackApiResponse } from '@biconomy/core-types';
/**
 * Verifying and Signing fallback gasless transactions
 */
export declare class FallbackGasTankAPI implements IFallbackAPI {
    readonly signingServiceUrl: string;
    readonly dappAPIKey: string;
    constructor(signingServiceUrl: string, dappAPIKey: string);
    getDappIdentifierAndSign(fallbackUserOp: Partial<FallbackUserOperation>): Promise<FallbackApiResponse>;
}
