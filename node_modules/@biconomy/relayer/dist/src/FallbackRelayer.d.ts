/// <reference types="node" />
import { JsonRpcProvider, TransactionResponse } from '@ethersproject/providers';
import { IFallbackRelayer } from '.';
import { RelayTransaction, FallbackRelayerOptions, ChainId } from '@biconomy/core-types';
import { EventEmitter } from 'isomorphic-ws';
/**
 * Relayer class that would be used via REST API to execute transactions
 */
export declare class FallbackRelayer implements IFallbackRelayer {
    #private;
    relayerNodeEthersProvider: {
        [chainId: number]: JsonRpcProvider;
    };
    constructor(options: FallbackRelayerOptions);
    setRelayerNodeEthersProvider(chainId: ChainId): void;
    relay(relayTransaction: RelayTransaction, engine: EventEmitter): Promise<TransactionResponse>;
}
