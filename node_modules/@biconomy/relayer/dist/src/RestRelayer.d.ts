/// <reference types="node" />
import { JsonRpcProvider } from '@ethersproject/providers';
import { IRelayer } from '.';
import { RelayTransaction, DeployWallet, RestRelayerOptions, FeeOptionsResponse, RelayResponse, ChainId } from '@biconomy/core-types';
import { EventEmitter } from 'isomorphic-ws';
/**
 * Relayer class that would be used via REST API to execute transactions
 */
export declare class RestRelayer implements IRelayer {
    #private;
    relayerNodeEthersProvider: {
        [chainId: number]: JsonRpcProvider;
    };
    constructor(options: RestRelayerOptions);
    setRelayerNodeEthersProvider(chainId: ChainId): void;
    prepareWalletDeploy(deployWallet: DeployWallet): {
        to: string;
        data: string;
    };
    relay(relayTransaction: RelayTransaction, engine: EventEmitter): Promise<RelayResponse>;
    getFeeOptions(chainId: number): Promise<FeeOptionsResponse>;
}
