import { TransactionResponse } from '@ethersproject/providers';
import { Signer as AbstractSigner } from 'ethers';
import { IRelayer } from '.';
import { DeployWallet, FeeOptionsResponse, RelayTransaction, RelayResponse } from '@biconomy/core-types';
export declare class LocalRelayer implements IRelayer {
    private signer;
    constructor(signer: AbstractSigner);
    deployWallet(deployWallet: DeployWallet): Promise<TransactionResponse>;
    prepareWalletDeploy(deployWallet: DeployWallet): {
        to: string;
        data: string;
    };
    relay(relayTransaction: RelayTransaction): Promise<RelayResponse>;
    getFeeOptions(chainId: number): Promise<FeeOptionsResponse>;
}
